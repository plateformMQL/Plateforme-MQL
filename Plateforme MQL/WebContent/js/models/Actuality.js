class Actuality {
   constructor(id, title, subtitle, description, event) {
       this.id = id;
       this.title = title;
       this.subtitle = subtitle;
       this.description = description;
       this.event = event;
   }
}

